export type Especialidade = { // export -> serve para poder usar o type em outros arquivos .ts
    id: number;
    nome: string;
    descricao?: string; // Significa que a propriedade é opcional
}



